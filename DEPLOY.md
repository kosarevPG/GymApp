# Инструкция по деплою на Render.com

## Быстрый старт

### 1. Подготовка к деплою

Убедитесь, что у вас есть:
- ✅ Аккаунт на GitHub
- ✅ Аккаунт на Render.com
- ✅ Telegram бот (создайте через @BotFather)
- ✅ Google Cloud проект с Service Account
- ✅ Google Таблица с листами LOG и EXERCISES

### 2. Загрузка кода на GitHub

```bash
# Инициализация репозитория (если еще не сделано)
git init
git add .
git commit -m "Initial commit: GymApp ready for production"

# Создайте репозиторий на GitHub и выполните:
git remote add origin https://github.com/YOUR_USERNAME/gymapp.git
git branch -M main
git push -u origin main
```

### 3. Деплой бэкенда на Render

1. Зайдите на https://render.com
2. Нажмите **New +** → **Web Service**
3. Подключите ваш GitHub репозиторий
4. Заполните настройки:
   - **Name**: `gymapp-backend`
   - **Region**: выберите ближайший
   - **Branch**: `main`
   - **Root Directory**: оставьте пустым
   - **Environment**: `Python 3`
   - **Build Command**: `cd back && pip install -r requirements.txt`
   - **Start Command**: `cd back && python bot.py`
5. В разделе **Environment Variables** добавьте:
   ```
   BOT_TOKEN=ваш_токен_бота
   SPREADSHEET_ID=id_вашей_таблицы
   GOOGLE_CREDENTIALS_JSON={"type":"service_account","project_id":"...","private_key_id":"...","private_key":"...","client_email":"...","client_id":"...","auth_uri":"...","token_uri":"...","auth_provider_x509_cert_url":"...","client_x509_cert_url":"..."}
   WEBAPP_URL=https://gymapp-frontend.onrender.com (обновите после деплоя фронтенда)
   ```
6. Нажмите **Create Web Service**
7. Дождитесь деплоя (5-10 минут)
8. Скопируйте URL бэкенда (например: `https://gymapp-backend.onrender.com`)

### 4. Деплой фронтенда на Render

1. На Render.com нажмите **New +** → **Static Site**
2. Подключите тот же GitHub репозиторий
3. Заполните настройки:
   - **Name**: `gymapp-frontend`
   - **Branch**: `main`
   - **Root Directory**: оставьте пустым
   - **Build Command**: `cd front && npm install && npm run build`
   - **Publish Directory**: `front/dist`
4. В разделе **Environment Variables** добавьте:
   ```
   VITE_API_BASE_URL=https://gymapp-backend.onrender.com
   ```
5. Нажмите **Create Static Site**
6. Дождитесь деплоя (3-5 минут)
7. Скопируйте URL фронтенда (например: `https://gymapp-frontend.onrender.com`)

### 5. Обновление настроек

1. Вернитесь в настройки бэкенда
2. Обновите `WEBAPP_URL` на URL вашего фронтенда
3. Сохраните (Render автоматически перезапустит)

### 6. Настройка Telegram бота

1. Откройте @BotFather в Telegram
2. Отправьте `/mybots`
3. Выберите вашего бота
4. Выберите **Bot Settings** → **Menu Button**
5. Нажмите **Configure Menu Button**
6. Введите URL фронтенда: `https://gymapp-frontend.onrender.com`
7. Готово! 🎉

## Проверка работы

1. Откройте вашего бота в Telegram
2. Нажмите кнопку меню (или отправьте `/start`)
3. Нажмите кнопку "🚀 Открыть GymApp"
4. Приложение должно открыться и работать!

## Важные замечания

- ⚠️ На бесплатном плане Render сервисы "засыпают" после 15 минут неактивности
- ⚠️ Первый запрос после "сна" может занять 30-60 секунд
- ⚠️ Для продакшена рекомендуется использовать платный план Render
- ⚠️ Не коммитьте `credentials.json` в Git (он в .gitignore)

## Troubleshooting

### Бэкенд не запускается
- Проверьте логи в Render Dashboard
- Убедитесь, что все переменные окружения установлены
- Проверьте формат `GOOGLE_CREDENTIALS_JSON` (должен быть валидный JSON)

### Фронтенд не подключается к бэкенду
- Проверьте `VITE_API_BASE_URL` в настройках фронтенда
- Убедитесь, что URL бэкенда правильный
- Проверьте CORS настройки (они уже настроены в bot.py)

### Telegram бот не открывает приложение
- Проверьте `WEBAPP_URL` в настройках бэкенда
- Убедитесь, что URL фронтенда правильный
- Проверьте настройки Menu Button в @BotFather

